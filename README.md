# Regular-Expression-Graph-Synthesis-Automata-Engine
A custom C++ regular expression engine that compiles patterns into NFAs, converts them to DFAs via subset construction, and executes string matching.
